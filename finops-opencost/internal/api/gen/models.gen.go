// Package gen provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.5.1 DO NOT EDIT.
package gen

import (
	"time"

	openapi_types "github.com/oapi-codegen/runtime/types"
)

const (
	BearerAuthScopes = "BearerAuth.Scopes"
)

// ComponentCost defines model for ComponentCost.
type ComponentCost struct {
	// Component Human-readable component name. Not stable across renames.
	Component string `json:"component"`

	// ComponentUid UID of the component this record belongs to.
	ComponentUid openapi_types.UUID `json:"componentUid"`

	// CpuCost CPU cost for this record's window.
	CpuCost float32 `json:"cpuCost"`

	// Efficiency Cost-weighted blend of CPU and memory utilization for this
	// record's window. A value of 0.85 means 85% of what was paid for
	// was actually used.
	Efficiency float32 `json:"efficiency"`

	// EndTime Exclusive end of the window this record covers. Equals the request
	// `endTime` when no granularity is set; otherwise the end of the
	// time bucket.
	EndTime time.Time `json:"endTime"`

	// Environment Human-readable environment name.
	Environment    string             `json:"environment"`
	EnvironmentUid openapi_types.UUID `json:"environmentUid"`

	// MemoryCost Memory cost for this record's window.
	MemoryCost float32 `json:"memoryCost"`

	// Namespace Namespace name. OpenChoreo namespaces do not have UIDs, so the
	// name is used here.
	Namespace string `json:"namespace"`

	// Project Human-readable project name.
	Project    string             `json:"project"`
	ProjectUid openapi_types.UUID `json:"projectUid"`

	// StartTime Inclusive start of the window this record covers. Equals the
	// request `startTime` when no granularity is set; otherwise the
	// start of the time bucket.
	StartTime time.Time `json:"startTime"`
}

// ComponentRecommendation defines model for ComponentRecommendation.
type ComponentRecommendation struct {
	// Component Human-readable component name.
	Component    string             `json:"component"`
	ComponentUid openapi_types.UUID `json:"componentUid"`

	// Current Resource requests, limits, and resulting cost. Resource fields use
	// Kubernetes quantity notation as strings (e.g. `"230m"` for CPU
	// millicores, `"250Mi"` for memory).
	Current ResourceProfile `json:"current"`

	// Environment Human-readable environment name.
	Environment    string             `json:"environment"`
	EnvironmentUid openapi_types.UUID `json:"environmentUid"`
	Namespace      string             `json:"namespace"`

	// Project Human-readable project name.
	Project    string             `json:"project"`
	ProjectUid openapi_types.UUID `json:"projectUid"`

	// Recommendation Resource requests, limits, and resulting cost. Resource fields use
	// Kubernetes quantity notation as strings (e.g. `"230m"` for CPU
	// millicores, `"250Mi"` for memory).
	Recommendation ResourceProfile `json:"recommendation"`
}

// CostResponse defines model for CostResponse.
type CostResponse struct {
	// Items Flat list of per-component cost records. Without `granularity`,
	// one record per component. With `granularity`, one record per
	// component per time bucket.
	Items []ComponentCost `json:"items"`
}

// Error defines model for Error.
type Error struct {
	Message string `json:"message"`
}

// RecommendationResponse defines model for RecommendationResponse.
type RecommendationResponse struct {
	// Items One recommendation per component in scope. A single element when
	// `componentUid` is specified; one per component in the project
	// otherwise. Uses the same array envelope as the cost endpoint.
	Items []ComponentRecommendation `json:"items"`
}

// ResourceProfile Resource requests, limits, and resulting cost. Resource fields use
// Kubernetes quantity notation as strings (e.g. `"230m"` for CPU
// millicores, `"250Mi"` for memory).
type ResourceProfile struct {
	// CpuCost CPU cost for the selected time window at these resource settings.
	CpuCost    float32 `json:"cpuCost"`
	CpuLimit   string  `json:"cpuLimit"`
	CpuRequest string  `json:"cpuRequest"`

	// MemoryCost Memory cost for the selected time window at these resource
	// settings.
	MemoryCost    float32 `json:"memoryCost"`
	MemoryLimit   string  `json:"memoryLimit"`
	MemoryRequest string  `json:"memoryRequest"`
}

// ComponentUid defines model for ComponentUid.
type ComponentUid = openapi_types.UUID

// EndTime defines model for EndTime.
type EndTime = time.Time

// EnvironmentUid defines model for EnvironmentUid.
type EnvironmentUid = openapi_types.UUID

// Granularity defines model for Granularity.
type Granularity = string

// Namespace defines model for Namespace.
type Namespace = string

// ProjectUid defines model for ProjectUid.
type ProjectUid = openapi_types.UUID

// StartTime defines model for StartTime.
type StartTime = time.Time

// BadRequest defines model for BadRequest.
type BadRequest = Error

// InternalError defines model for InternalError.
type InternalError = Error

// NotFound defines model for NotFound.
type NotFound = Error

// Unauthorized defines model for Unauthorized.
type Unauthorized = Error

// GetComponentCostsParams defines parameters for GetComponentCosts.
type GetComponentCostsParams struct {
	// ProjectUid Project UID. When set, narrows the response to components in this
	// project.
	ProjectUid *ProjectUid `form:"projectUid,omitempty" json:"projectUid,omitempty"`

	// ComponentUid Component UID. When set, narrows the response to this single component.
	// Requires `projectUid` to also be set.
	ComponentUid *ComponentUid `form:"componentUid,omitempty" json:"componentUid,omitempty"`

	// StartTime Inclusive lower bound of the cost window. RFC 3339 timestamp with
	// second precision (sub-second components are not supported and will
	// be rejected).
	StartTime StartTime `form:"startTime" json:"startTime"`

	// EndTime Exclusive upper bound of the cost window. RFC 3339 timestamp with
	// second precision (sub-second components are not supported and will
	// be rejected). Must be strictly greater than `startTime`.
	EndTime EndTime `form:"endTime" json:"endTime"`

	// Granularity When present, each component is split into one record per time bucket
	// of this size. Records with the same `componentUid` and differing time
	// windows represent that component across multiple buckets.
	//
	// The value uses `<count><unit>` notation, where `count` is a positive
	// integer and `unit` is one of `h` (hours), `d` (days), or `w` (weeks).
	// For example: `1h`, `2d`, `3w`.
	Granularity *Granularity `form:"granularity,omitempty" json:"granularity,omitempty"`
}

// GetRecommendationsParams defines parameters for GetRecommendations.
type GetRecommendationsParams struct {
	// StartTime Inclusive lower bound of the cost window. RFC 3339 timestamp with
	// second precision (sub-second components are not supported and will
	// be rejected).
	StartTime StartTime `form:"startTime" json:"startTime"`

	// EndTime Exclusive upper bound of the cost window. RFC 3339 timestamp with
	// second precision (sub-second components are not supported and will
	// be rejected). Must be strictly greater than `startTime`.
	EndTime EndTime `form:"endTime" json:"endTime"`

	// ProjectUid Project UID. When set, narrows the result to components in this
	// project. When omitted, recommendations for all components in the
	// environment are returned.
	ProjectUid *openapi_types.UUID `form:"projectUid,omitempty" json:"projectUid,omitempty"`

	// ComponentUid Component UID. When set, narrows the result to this single
	// component. Requires `projectUid` to also be set.
	ComponentUid *openapi_types.UUID `form:"componentUid,omitempty" json:"componentUid,omitempty"`

	// Environment Environment name. Required for the same reason as `namespace`.
	Environment string `form:"environment" json:"environment"`

	// Project Project name. Required only when `projectUid` is supplied, since
	// the adapter needs it for the name-based metrics call.
	Project *string `form:"project,omitempty" json:"project,omitempty"`

	// Component Component name. Required only when `componentUid` is supplied,
	// since the adapter needs it for the name-based metrics call.
	Component *string `form:"component,omitempty" json:"component,omitempty"`
}
