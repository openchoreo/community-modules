Reference
- observability-logs-cloudwatch/*

We need to remove instance name from the helm chart and improve readme like in cloudwatch logs module. 
- Enable alerts by default (check)
- Support for multi-cluster works fine